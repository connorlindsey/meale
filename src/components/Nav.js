import React from 'react'
import { Button } from "../style/Button"

const Nav = () => {
  return (
    <header>
      <nav>
        <Button>Go to Your Meale</Button>
      </nav>
    </header>
  )
}

export default Nav
